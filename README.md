## php Even Dispatcher Package
It is simple php Even dispatcher package

### Install
```bash
    composer require sujan/even-dispatcher
```

### Use
```php
    $dispatcher = new Dispatcher(
        listners:[],
        log:[]
    );
    // Add even with listners
```